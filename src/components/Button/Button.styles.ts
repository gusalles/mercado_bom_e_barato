import { Button, styled } from '@mui/material';

export const StyledButton = styled(Button)(() => ({
  marginTop: '16px',
  borderRadius: '12px',
  color: '#000',
  textTransform: 'initial',
  fontWeight: 600,
  fontSize: '1rem',
  height: '56px',
}));
